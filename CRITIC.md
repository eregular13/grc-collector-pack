# CRITIC — cycle 28 (compose scanner-free statics)

**8/10** — zero P0/P1. Catalog **unchanged**: **111 / 32 / 30 / 81**. Farm + dropbox compose are operator skeletons (SCOPE / work / tool-bin). Statics fail closed on LICENSE-LOCK embeds and wrap POST. `make dropbox-compose` / `make farm-compose` stamp **ABSENT** after static PASS (no Docker CLI) — not a runtime pass. Labs green. Wrap review-only.

−1 compose runtime still absent (no Docker CLI).  
−1 live BYO still plan-only here (nmap/nessus missing). 30 invoke adapters ≠ 100 running binaries.

```json
{"pytest": 168, "pytest_skipped": 1, "farm_slots": 111, "wired": 32, "invoke": 30, "file_drop": 81, "assets_empty_in": 62, "poam": 61, "farm_lab": "pass", "host_lab": "pass", "dropbox_lab": "pass", "compose_lab": "absent", "scanner_free": true, "wrap": "review-only"}
```
