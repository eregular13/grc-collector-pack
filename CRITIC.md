# CRITIC — cycle 65 (Argus wrap-dead)

**8/10** — zero P0/P1. Catalog **unchanged**: **111 / 32 / 30 / 81**. No new parsers. RiskReady wrap stays **dead** forever (no login / no assets / no incidents / no evidence POST — not only `/api/risks`). Farm SOP never points at a RiskReady write and never inherits Windows/Origin mock rehearsal (`:18080` / mock_sink). STATUS `wrap: review-only`. Paying-day **FAIL**. Compose **ABSENT** (hole, not a PASS). DEMO ≠ client. Hexstrike pattern-only. PATH invoke BYO under SCOPE only. Host lab 64/79/19/27 poam 82. e2e 64/80/19 poam 82 demo true. pytest **307**.

−1 compose runtime still absent (no Docker CLI).  
−1 stubs are DEMO, not real nmap/nessus.

```json
{"pytest": 307, "pytest_skipped": 1, "farm_slots": 111, "wired": 32, "invoke": 30, "file_drop": 81, "farm_toolbin_e2e": "pass", "farm_lab": "pass", "host_lab": "pass", "dropbox_lab": "pass", "compose_lab": "absent", "scanner_free": true, "wrap": "review-only", "paying_day": "FAIL", "license_lock_will_run": "never"}
```
