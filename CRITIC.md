# CRITIC — cycle 58 (smbmap file-drop polish)

**8/10** — zero P0/P1. Catalog **unchanged**: **111 / 32 / 30 / 81**. Layer C parses operator-landed smbmap share tables under `in/nmap/` (`[+] IP:` / Disk + Permissions). Hosts become assets. READ/WRITE shares become exposure findings mapped to existing SMB POA&M. Empty / NO ACCESS invent nothing. Detect does not steal nmap / arp-scan / nbtscan. Demo `smbmap.txt` attaches writable C$ to existing `filesrv.corp.local`. No smbmap/smbclient subprocess. No live SMB. No credentials. Slot stays `file_drop`. nbtscan cycle 57 stands. e2e assets still 64. Host lab 64/76/19/27 poam 79. e2e 64/77/19 poam 79 demo true. pytest **288**. Docs/e2e stand. No live internet. Compose still ABSENT. Wrap review-only. No slot inflation.

−1 compose runtime still absent (no Docker CLI).  
−1 stubs are DEMO, not real nmap/nessus. 30 invoke adapters ≠ 100 running binaries.

```json
{"pytest": 288, "pytest_skipped": 1, "farm_slots": 111, "wired": 32, "invoke": 30, "file_drop": 81, "farm_toolbin_e2e": "pass", "farm_lab": "pass", "host_lab": "pass", "dropbox_lab": "pass", "compose_lab": "absent", "scanner_free": true, "wrap": "review-only"}
```
