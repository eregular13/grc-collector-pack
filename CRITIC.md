# CRITIC — cycle 57 (nbtscan file-drop polish)

**8/10** — zero P0/P1. Catalog **unchanged**: **111 / 32 / 30 / 81**. Layer C parses operator-landed nbtscan name/IP tables under `in/nmap/` (`Doing NBT name scan` / IP + NetBIOS + `<server>`). Hosts become assets only. Empty / header-only invent nothing. Detect does not steal arp-scan / netdiscover / fping. Demo `nbtscan.txt` attaches to existing `filesrv.corp.local` (no new findings). No nbtscan subprocess. No live NetBIOS. Slot stays `file_drop`. netdiscover cycle 56 stands. e2e assets still 64. Host lab 64/75/19/27 poam 78. e2e 64/76/19 poam 78 demo true. pytest **284**. Docs/e2e stand. No live internet. Compose still ABSENT. Wrap review-only. No slot inflation.

−1 compose runtime still absent (no Docker CLI).  
−1 stubs are DEMO, not real nmap/nessus. 30 invoke adapters ≠ 100 running binaries.

```json
{"pytest": 284, "pytest_skipped": 1, "farm_slots": 111, "wired": 32, "invoke": 30, "file_drop": 81, "farm_toolbin_e2e": "pass", "farm_lab": "pass", "host_lab": "pass", "dropbox_lab": "pass", "compose_lab": "absent", "scanner_free": true, "wrap": "review-only"}
```
