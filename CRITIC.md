# CRITIC — cycle 30 (deepen FARM_TOOL_BIN DEMO stubs)

**8/10** — zero P0/P1. Catalog **unchanged**: **111 / 32 / 30 / 81**. Lab stubs now include nessus / nessuscli / testssl / testssl.sh / lynis (DEMO, no network, no ELF). Deepen + external-adjacent `select_stage_slots` `will_run` true; orchestrate external stage still plan-only. LICENSE-LOCK names still refuse subprocess. `make farm-toolbin-lab` asserts nmap+curl. Compose still ABSENT. Wrap review-only.

−1 compose runtime still absent (no Docker CLI).  
−1 stubs are DEMO, not real nmap/nessus. 30 invoke adapters ≠ 100 running binaries.

```json
{"pytest": 176, "pytest_skipped": 1, "farm_slots": 111, "wired": 32, "invoke": 30, "file_drop": 81, "assets_empty_in": 62, "poam": 61, "farm_lab": "pass", "host_lab": "pass", "dropbox_lab": "pass", "compose_lab": "absent", "scanner_free": true, "wrap": "review-only"}
```
