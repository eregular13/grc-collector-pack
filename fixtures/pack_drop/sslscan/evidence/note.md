SAMPLE/DEMO — not a client estate.
Covey sslscan pack_drop evidence stub. Parse-only file_drop into in/nmap/.
Stdout/XML-class export_pack (hosts + open-port observations).
XML artifact aligns with shared/sslscan.py + fixtures/demo/vuln/sslscan.xml
(SAMPLE). Not a live scan. Not a client KEEP.
